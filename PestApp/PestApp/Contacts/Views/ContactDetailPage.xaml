<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PestApp.Contacts.Views.ContactDetailPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Label Text="Name" />
        <Entry Grid.Column="1" Text="{Binding Contact.Name}" />
        <Label Grid.Row="1" Text="Address" />
        <Entry Grid.Row="1" Grid.Column="1" Text="{Binding Contact.Address}" />
    </Grid>
</ContentPage>